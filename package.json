{
  "name": "curl-runner",
  "version": "0.1.0",
  "description": "Paste a curl command, fill in {{variables}}, and run it on a loop with a delay. Streams responses live.",
  "type": "module",
  "main": "server.js",
  "scripts": {
    "start": "node server.js"
  },
  "engines": {
    "node": ">=20"
  },
  "dependencies": {
    "express": "^4.19.2"
  }
}
